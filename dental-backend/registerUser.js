'use strict';

console.error(
    'registerUser.js is deprecated because the shared appUser identity bypasses Phase 2 actor binding. ' +
    'Run "npm run fabric:register-identities" instead.'
);
process.exit(1);
